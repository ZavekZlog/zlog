/**
 * Dashboard content grid — shared by masthead Sign out row and card column.
 * Sign out right-edge must match the card column right-edge.
 */
export const DASHBOARD_CONTENT_GRID = {
  maxWidth: 600,
  padX: 20,
  contentTop: 16,
  contentBottom: 24,
}
