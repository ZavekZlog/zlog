/* Fixture: unregistered protected-rule disable (must FAIL the exception gate). */
/* eslint-disable react-hooks/set-state-in-effect */
export function UnregisteredFixture() {
  return null
}
/* eslint-enable react-hooks/set-state-in-effect */
